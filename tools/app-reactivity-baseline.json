{
  "_doc": "Files that hand-roll a change loop on the coarse `vaultEntities.onChange` signal instead of going through @brainstorm-os/react-yjs's useVaultEntities/useLiveEntities. A RATCHET enforced by tools/check-app-reactivity.mjs: it may only shrink. New apps/files MUST use the shared hooks (the gate rejects additions). As apps migrate, delete the migrated file's line. When empty, delete this baseline and make the gate an absolute ban. NOTE: passing `onChange` INTO a sanctioned sink (useLiveEntities / useVaultEntities / setEntityIndexSource) is compliant and is no longer flagged — see SANCTIONED_SINKS in the checker. Two files were wrongly listed here on that basis and have been removed.",
  "files": ["apps/database/src/app.ts", "apps/files/src/store/use-files-store.ts"],
  "whyStillBaselined": {
    "apps/database/src/app.ts": "Real debt, and the largest: an imperative-DOM app (5.3k lines, 26 onChange refs). Its migration is the all-apps-React track, not a reactivity-only change.",
    "apps/files/src/store/use-files-store.ts": "BLOCKED on a missing shared-hook seam, not laziness. `loadFromVault()` RECONCILES over the previous view (tree expansion, nav history, resolved openers) and deliberately keeps the current view when `vaultEntities.list` throws. `useLiveEntities` offers only `equals`, which can suppress a render but cannot merge prev into next."
  },
  "blocker": "RESOLVED for notes. `reconcile(prev, next) => T` now exists on the shared reactivity options (query-store + useLiveEntities) and landed WITH its first real migration, per the rule it was deferred under. `apps/files/src/store/use-files-store.ts` is now unblocked too — its view-state merge (tree expansion, nav history, resolved openers) is the same shape as the notes pin — and is the next one to migrate. `apps/database/src/app.ts` stays: it is the all-apps-React track, not a reactivity-only change."
}
