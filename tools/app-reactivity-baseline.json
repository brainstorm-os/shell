{
	"_doc": "Files that hand-roll a change loop on the coarse `vaultEntities.onChange` signal instead of going through @brainstorm-os/react-yjs's useVaultEntities/useLiveEntities. A RATCHET enforced by tools/check-app-reactivity.mjs: it may only shrink. New apps/files MUST use the shared hooks (the gate rejects additions). As apps migrate, delete the migrated file's line. When empty, delete this baseline and make the gate an absolute ban. NOTE: passing `onChange` INTO a sanctioned sink (useLiveEntities / useVaultEntities / setEntityIndexSource) is compliant and is no longer flagged — see SANCTIONED_SINKS in the checker. Two files were wrongly listed here on that basis and have been removed.",
	"files": [
		"apps/database/src/app.ts"
	],
	"whyStillBaselined": {
		"apps/database/src/app.ts": "Real debt, and the largest: an imperative-DOM app (5.3k lines, 26 onChange refs). Its migration is the all-apps-React track, not a reactivity-only change."
	},
	"blocker": "Only `apps/database/src/app.ts` remains, and it is NOT blocked on a missing seam — it is the all-apps-React migration track (5.3k lines of imperative DOM, 26 onChange refs). HISTORY, because the earlier note was wrong and cost a cycle: notes AND files were both recorded as blocked on a `reconcile(prev, next)` option. That was true for notes (it pins the open note and in-flight saves over the disk snapshot) and `reconcile` shipped with that migration. It was NOT true for files: reading `loadFromVault` shows it never merges prev into next — it runs a side effect (resolveOpeners) before a fingerprint short-circuit, and keeps the current view when list() throws. Those are `equals` and `onError`, which the shared store had all along. Files was simply un-migrated, and the migration needed no new seam."
}
