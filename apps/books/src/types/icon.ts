/**
 * Canonical universal icon shape,
 * re-exported from `@brainstorm-os/sdk-types` — the 9.21.1-scaffold local
 * mirror is gone now that the React conversion wires the real SDK types
 * (the swap the mirror's comment always promised). Existing imports keep
 * their `../types/icon` path.
 */

export { IconKind } from "@brainstorm-os/sdk-types";
export type { Icon } from "@brainstorm-os/sdk-types";
