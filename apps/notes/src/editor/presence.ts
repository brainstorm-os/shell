/**
 * Local collaborator identity for the Notes editor (B11.9) — thin bridge over
 * the shared `@brainstorm-os/editor` helpers (extracted at copy two when Journal
 * adopted comments and needed the same display-name pref + colour keying).
 */

export { localPresence, localPresenceName } from "@brainstorm-os/editor";
