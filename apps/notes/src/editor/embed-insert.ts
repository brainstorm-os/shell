/** Re-export shim — the embed-insert helpers now live in
 *  `@brainstorm/editor` alongside `BlockEmbedNode` (F-070 embed parity). */
export { type EmbedInsertion, applyEmbedInsertion } from "@brainstorm/editor";
