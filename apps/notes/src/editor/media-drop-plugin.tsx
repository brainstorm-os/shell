/**
 * MediaDropPlugin — extracted to `@brainstorm-os/editor` so every editor
 * surface gets drag-drop + paste of media files. Re-exported here for the
 * existing Notes import site (`editor.tsx`).
 */

export { MediaDropPlugin } from "@brainstorm-os/editor";
