/**
 * MediaInspectorPlugin — extracted to `@brainstorm-os/editor` (the shared
 * media stack). Re-exported here for the existing Notes import site
 * (`editor.tsx`).
 */

export { MediaInspectorPlugin } from "@brainstorm-os/editor";
