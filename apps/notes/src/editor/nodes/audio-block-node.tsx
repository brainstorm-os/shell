/**
 * AudioBlockNode — extracted to `@brainstorm-os/editor` (the shared media
 * stack). Re-exported here for Notes' node set + commands.
 */

export {
	$createAudioBlockNode,
	$isAudioBlockNode,
	AUDIO_BLOCK_TYPE,
	AudioBlockNode,
	type SerializedAudioBlockNode,
} from "@brainstorm-os/editor";
