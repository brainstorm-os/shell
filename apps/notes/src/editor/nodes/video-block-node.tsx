/**
 * VideoBlockNode — extracted to `@brainstorm-os/editor` (the shared media
 * stack). Re-exported here for Notes' node set + commands.
 */

export {
	$createVideoBlockNode,
	$isVideoBlockNode,
	VIDEO_BLOCK_TYPE,
	VideoBlockNode,
	type SerializedVideoBlockNode,
} from "@brainstorm-os/editor";
