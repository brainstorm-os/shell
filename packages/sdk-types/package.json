{
  "name": "@brainstorm-os/sdk-types",
  "version": "0.0.1",
  "description": "Type declarations for the Brainstorm app SDK. Runtime is injected by the shell; this package is types only.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --outDir dist --tsconfig tsconfig.build.json && node ../../scripts/prepare-publish.mjs sdk-types",
    "prepublishOnly": "bun run build"
  }
}
