{
  "name": "@brainstorm-os/relay-server",
  "version": "0.0.1",
  "description": "Blind relay for Brainstorm Stage 10.4 — opaque-bytes fan-out over WebSocket.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "bun --bun run bin/relay.ts"
  }
}
