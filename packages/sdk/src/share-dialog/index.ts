/**
 * `@brainstorm-os/sdk/share-dialog` — the shared multi-user share surface
 * (Collab-C5): member list + add-by-invite-code + revoke + your-invite-code,
 * over `services.sharing` + `services.roster`. Import the CSS via
 * `@brainstorm-os/sdk/share-dialog.css`.
 */

export { ShareDialog, type ShareDialogProps, type ShareDialogLabels } from "./share-dialog";
